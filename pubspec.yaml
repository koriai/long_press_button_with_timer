name: long_press_button_with_timer
description: A long press sensitive button with visible timer (e.g. Long press for 5 seconds to delete account)

# publish_to: 'none'
repository: https://github.com/koriai/long_press_button_with_timer
version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
